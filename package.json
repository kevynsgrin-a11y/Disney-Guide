{
  "name": "evergreen-theme-park-guide",
  "version": "1.0.0",
  "private": true,
  "description": "Evergreen Theme Park Guide — a static, data-driven guide to the six US Disney parks.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/validate.mjs && node scripts/validate-seasonal.mjs && node scripts/factcheck.mjs && node scripts/factcheck-seasonal.mjs && node src/build.mjs",
    "build:fast": "node src/build.mjs",
    "validate": "node scripts/validate.mjs && node scripts/validate-seasonal.mjs",
    "factcheck": "node scripts/factcheck.mjs && node scripts/factcheck-seasonal.mjs",
    "test": "node --test test/*.test.mjs",
    "serve": "node scripts/serve.mjs dist/disney",
    "dev": "npm run build && npm run serve",
    "audit:site": "node scripts/audit.mjs",
    "check": "npm run test && npm run build && npm run audit:site",
    "maps:png": "node scripts/render-map-pngs.mjs"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "playwright-core": "^1.62.1"
  }
}
